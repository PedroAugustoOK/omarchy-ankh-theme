name: Validate Omarchy Ankh

on:
  push:
  pull_request:

jobs:
  theme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate theme assets and palette contracts
        run: python3 scripts/validate-theme.py
