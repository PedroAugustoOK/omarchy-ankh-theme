# Ankh companion configuration for bat.
# Copy or merge into ~/.config/bat/config. `ansi` keeps syntax colors aligned
# with the terminal palette generated by Omarchy from colors.toml.
--theme="ansi"
--style="numbers,changes,header,grid"
--italic-text=always
